# Execute & Fix Tests — On-Demand Pipeline
#
# Standalone pipeline that runs Execute_And_Fix_Tests on any scope,
# without requiring the full AI generation stage.
#
# Use this to:
#   • Re-run and fix tests after a deployment or env change
#   • Run a specific module, tag, or single spec
#   • Run regression / smoke suites on demand
#
# Trigger: manual queue only — no automatic triggers
#
# Required variable group: "ai-generation-secrets"
#   ANTHROPIC_API_KEY, AZURE_DEVOPS_ORG_URL, AZURE_PERSONAL_ACCESS_TOKEN,
#   AZURE_PROJECT_NAME, BASE_URL
#
# See docs/pipelines/execute-fix.md for full setup and usage guide.

trigger: none   # manual queue only

parameters:
  - name: testScope
    type: string
    default: "(skip)"
    displayName: >
      Test scope for Execute_And_Fix_Tests ((skip) to skip execution).
      • @regression or @smoke  — grep tag filter
      • Projects               — module name → tests/generated/Projects/
      • All tests              — run entire tests/generated/ suite
      • (skip)                 — skip execution

variables:
  - group: ai-generation-secrets
  - name: NODE_VERSION
    value: "18.x"
  - name: CLAUDE_CLI
    value: "@anthropic-ai/claude-code"

stages:
  - stage: ExecuteAndFix
    displayName: "Execute & Fix Tests"
    jobs:
      - job: TestAndFix
        displayName: "Run Execute_And_Fix_Tests"
        pool:
          vmImage: ubuntu-latest
        steps:
          - checkout: self
            persistCredentials: true
            displayName: "Checkout source"

          - task: NodeTool@0
            displayName: "Install Node.js $(NODE_VERSION)"
            inputs:
              versionSpec: $(NODE_VERSION)

          - script: npm ci
            displayName: "Install project dependencies"

          - script: npx playwright install --with-deps chromium
            displayName: "Install Playwright browsers"

          - script: npm install -g $(CLAUDE_CLI)
            displayName: "Install Claude Code CLI"

          # Resolve the scope parameter:
          #   (skip)        → skip with warning
          #   All tests     → tests/generated/
          #   @tag          → passed as-is (grep filter for the skill)
          #   tests/…       → passed as-is (explicit path)
          #   anything else → tests/generated/<scope>/  (module name shorthand)
          - script: |
              set -euo pipefail
              SCOPE="${TEST_SCOPE_PARAM}"
              if [ "$SCOPE" = "(skip)" ]; then
                echo "##vso[task.logissue type=warning]testScope is (skip) — skipping execution."
                echo "##vso[task.complete result=Skipped]No testScope provided"
                exit 0
              fi
              if [ "$SCOPE" = "All tests" ] || [ "$SCOPE" = "all tests" ]; then
                SCOPE="tests/generated/"
              elif [[ "$SCOPE" != @* ]] && [[ "$SCOPE" != tests/* ]]; then
                SCOPE="tests/generated/$SCOPE/"
              fi
              echo "Resolved test scope: $SCOPE"
              echo "##vso[task.setvariable variable=ResolvedScope]$SCOPE"
            displayName: "Resolve test scope"
            env:
              TEST_SCOPE_PARAM: "${{ parameters.testScope }}"

          - script: |
              set -euo pipefail
              printf '/Execute_And_Fix_Tests\n%s' "$RESOLVED_SCOPE" \
                | claude --dangerously-skip-permissions --output-format stream-json --verbose -p
            displayName: "Run Execute_And_Fix_Tests"
            timeoutInMinutes: 60
            env:
              ANTHROPIC_API_KEY: $(ANTHROPIC_API_KEY)
              AZURE_DEVOPS_ORG_URL: $(AZURE_DEVOPS_ORG_URL)
              AZURE_PERSONAL_ACCESS_TOKEN: $(AZURE_PERSONAL_ACCESS_TOKEN)
              AZURE_PROJECT_NAME: $(AZURE_PROJECT_NAME)
              BASE_URL: $(BASE_URL)
              RESOLVED_SCOPE: $(ResolvedScope)
              NO_COLOR: "1"
              TERM: dumb

          - task: PublishTestResults@2
            displayName: "Publish test results (JUnit)"
            condition: always()
            inputs:
              testResultsFormat: JUnit
              testResultsFiles: "**/playwright-report/results.xml"
              failTaskOnFailedTests: false

          - task: PublishBuildArtifacts@1
            displayName: "Publish test reports"
            condition: always()
            inputs:
              PathtoPublish: reports
              ArtifactName: test-reports

          - task: PublishBuildArtifacts@1
            displayName: "Publish Playwright HTML report"
            condition: always()
            inputs:
              PathtoPublish: playwright-report
              ArtifactName: playwright-report

          - script: |
              if [ -f reports/latest-execution.json ]; then
                echo "========== Test Execution Summary =========="
                cat reports/latest-execution.json
              fi
            displayName: "Display test summary"
            condition: always()
